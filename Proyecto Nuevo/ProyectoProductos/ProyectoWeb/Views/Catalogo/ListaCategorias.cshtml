@model List<ET.Categoria>
@{
    ViewBag.Title = "ListaCategorias";
}

<div class="row">
    <div class="col-md-2 pull-left">
        <div>
            <h3>Categorías</h3>
            @foreach (var c in Model)
            {
                <a href="~/Catalogo/ListaArticulos/@c.Id">@c.Nombre</a><br />
            }
        </div>
    </div>
    <div class="col-md-10 pull-right">
        <h2>Categorías</h2>
        <br>
        @foreach (var c in Model)
        {

            <div class="col-sm-6 col-md-3" style="padding-bottom:20px;">
                <a href="~/Catalogo/ListaArticulos/@c.Id">
                    <div class="thumbnail">
                        <img src=@("/Imagenes/Categorias/"+ c.Img) alt="Imagen de Prueba" style="width:100%; height:100%;">

                        <div class="caption">
                            <center>@c.Nombre</center>
                        </div>
                    </div>
                </a>
            </div>
        }
    </div>
</div>

